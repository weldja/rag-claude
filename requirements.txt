# ── Core RAG stack ──────────────────────────────────────────────────
streamlit>=1.35.0
langchain>=0.2.0
langchain-community>=0.2.0
langchain-huggingface>=0.0.3

# ── Vector database ──────────────────────────────────────────────────
pgvector>=0.2.5
psycopg2-binary>=2.9.9

# ── Embeddings (local, no API cost) ─────────────────────────────────
# BAAI/bge-small-en-v1.5 outperforms all-MiniLM on RAG benchmarks
sentence-transformers>=3.0.0
transformers>=4.40.0
torch>=2.2.0

# ── LLM: Claude API ──────────────────────────────────────────────────
anthropic>=0.28.0

# ── Document loaders ─────────────────────────────────────────────────
pypdf>=4.0.0                      # PDF
python-docx>=1.1.0                # DOCX
docx2txt>=0.8                     # DOCX (langchain loader uses this)
python-pptx>=0.6.23               # PPTX
unstructured>=0.14.0              # PPTX loader backend
openpyxl>=3.1.0                   # XLSX

# ── Data utilities ───────────────────────────────────────────────────
pandas>=2.1.0
numpy>=1.26.0

# ── Dev / environment ────────────────────────────────────────────────
python-dotenv>=1.0.0
pyyaml>=6.0
